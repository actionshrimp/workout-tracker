language: clojure
lein: lein2

install:
    - cd client
    - npm install -q
    - npm install -q -g grunt-cli
    - npm install -q -g bower
    - bower install
    - bundle install
    - cd ..

script:
    - cd client
    - grunt
    - cd ..
    - cd server
    - lein2 test
